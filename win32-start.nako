#--------------------------------------------------------------------------
# なでしこPHP3変換・実行プログラム(Windows版)
#--------------------------------------------------------------------------
# 2021年8月10日更新 EZNAVI.net
# 2022年5月21日更新 クジラ飛行机
#--------------------------------------------------------------------------
# 自由に変更等してください。（転載 引用自由）
#--------------------------------------------------------------------------
母艦のタイトル＝「なでしこ3PHP - 補助ツール」

PHP=「{母艦パス}php8\php.exe」
PHP_INI=「{母艦パス}php.ini」
PHPNAKO=「node "{母艦パス}node_modules\nadesiko3php\src\phpnako.js"」
NODE=「{母艦パス}nodejs16\node.exe」
NPM=「{母艦パス}nodejs16\npm.cmd」

PHPダウンロードURL=「https://windows.php.net/downloads/releases/archives/php-8.1.5-Win32-vs16-x86.zip」
NODEダウンロードURL=「https://nodejs.org/dist/v16.15.0/node-v16.15.0-win-x86.zip」

#--------------------------------------------------------------------------
『なでしこ3PHPのセットアップ - 上から順にクリックすると設定が完了します。』を表示
#--------------------------------------------------------------------------
PHPダウンロードボタンとはボタン
そのテキスト＝『PHPダウンロード』
その幅＝『200』
PHPダウンロードボタンをクリックした時は
　　ZIPFILE=「{母艦パス}php8.zip」
　　もし(ZIPFILEが存在)ならば
　　　　＃
　　違えば
　　　　PHPダウンロードURLをZIPFILEへHTTPダウンロード。
　　ZIPFILEを「{母艦パス}php8」へ解凍。
#--------------------------------------------------------------------------
NODEダウンロードボタンとはボタン
そのテキスト＝『Node.jsダウンロード』
その幅＝『200』
NODEダウンロードボタンをクリックした時は
　　ZIPFILE=「{母艦パス}nodejs16.zip」
　　もし(ZIPFILEが存在)ならば
　　　　＃
　　違えば
　　　　NODEダウンロードURLをZIPFILEへHTTPダウンロード。
　　ZIPFILEを「{母艦パス}nodejs16tmp」へ解凍。
　　「{母艦パス}nodejs16tmp\node-v16.15.0-win-x86」を「{母艦パス}nodejs16」へファイル移動。
　　「{母艦パス}nodejs16tmp」をファイル削除。
#--------------------------------------------------------------------------
更新ボタンとはボタン
そのテキスト＝『なでしこ3PHPをダウンロード』
その幅＝『200』
更新ボタンをクリックした時は
　　「cmd.exe /k cd "{母艦パス}" & "{NPM}" install nadesiko3php」を起動
#--------------------------------------------------------------------------
『なでしこ３をPHPに変換』を表示
#--------------------------------------------------------------------------
PHP変換ボタンとはボタン
そのテキスト＝『手動でPHPファイルに変換』
その幅＝『200』
PHP変換ボタンをクリックした時は
　　「.nako3」のファイル選択
　　変換ファイル＝それ
　　「cmd.exe /k {PHPNAKO} -c "｛変換ファイル｝"」を起動

#--------------------------------------------------------------------------
#ファイルをPHPに自動変換（監視モード）
#--------------------------------------------------------------------------
#※.nako3ファイルを編集保存すると自動でphpも変更されます。
自動ボタンとはボタン
そのテキスト＝『監視モード（自動）を起動』
その幅＝『200』
自動ボタンをクリックした時は
　　フォルダ選択
　　指定フォルダ＝それ
　　「cmd.exe /k cd "{指定フォルダ}"& {PHPNAKO} --watch」を起動

『ローカルサーバーを起動・表示』を表示

#--------------------------------------------------------------------------
#ローカルサーバー起動
#--------------------------------------------------------------------------
#※コマンドを終了させればサーバー終了となります
#※http://localhost:8888/〇〇.phpで開いてください
指定ボタンとはボタン
そのテキスト＝『ローカルサーバー起動』
その幅＝『200』
指定ボタンをクリックした時は
　　フォルダ選択
　　指定フォルダ＝それ
　　「cmd.exe /k cd "｛指定フォルダ｝"& "{PHP}" -S 0.0.0.0:8888 -c "{PHP_INI}"」を起動

#--------------------------------------------------------------------------
#Edgeでローカルサーバーを表示
#--------------------------------------------------------------------------
#※http://localhost:8888/で表示します
インターネットボタンとはボタン
そのテキスト＝『ローカルサーバー表示』
その幅＝『200』
インターネットボタンをクリックした時は
　　「cmd.exe /c start Microsoft-Edge:http://localhost:8888/」を起動

#--------------------------------------------------------------------------
#念のため
#--------------------------------------------------------------------------
『予備』を表示
コマンドボタンとはボタン
そのテキスト＝『コマンドを起動』
その幅＝『200』
コマンドボタンをクリックした時は
　　「cmd.exe」を起動。

公式ボタンとはボタン
そのテキスト＝『なでしこ公式サイト』
その幅＝『200』
公式ボタンをクリックした時は
　　「cmd.exe /c start Microsoft-Edge:https://nadesi.com」を起動

